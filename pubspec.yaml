name: agent_logger
description: package to create and read application logs in the application itself.
version: 0.0.5
homepage: https://github.com/timaZapadniy/agent_logger

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  logger: ^2.0.2+1
  path_provider: ^2.1.1
  sensors_plus: ^3.1.0
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


 